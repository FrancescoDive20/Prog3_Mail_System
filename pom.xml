<project xmlns="http://maven.apache.org/POM/4.0.0"

         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"

         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0

                             https://maven.apache.org/xsd/maven-4.0.0.xsd">


    <modelVersion>4.0.0</modelVersion>


    <!--

      =======================================================================

      POM Aggregatore (Parent POM)

      =======================================================================

      Definisce l'architettura multimodulo del progetto distribuito.

      Centralizza la gestione delle versioni, delle dipendenze e del ciclo di 

      build per garantire totale coerenza strutturale tra i vari moduli.

      =======================================================================

    -->

    <groupId>it.unito</groupId>

    <artifactId>ProgettoMail</artifactId>

    <version>1.0-SNAPSHOT</version>

    <packaging>pom</packaging>


    <!--

      Separazione delle responsabilità (Separation of Concerns).

      Permette la compilazione simultanea dell'intera infrastruttura 

      tramite un singolo ciclo di build Maven.

    -->

    <modules>

        <module>protocolloCondiviso</module>

        <module>mailServer</module>

        <module>mailClient</module>

    </modules>


    <!-- Costanti globali di progetto: versioning e target di compilazione -->

    <properties>

        <java.version>17</java.version>

        <jackson.version>2.17.0</jackson.version>

        <javafx.version>21.0.2</javafx.version>

        <maven.compiler.source>${java.version}</maven.compiler.source>

        <maven.compiler.target>${java.version}</maven.compiler.target>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    </properties>


    <!--

      Gestione centralizzata delle dipendenze.

      I sottomoduli erediteranno queste versioni senza doverle dichiarare,

      prevenendo conflitti di libreria a runtime (Dependency Hell).

    -->

    <dependencyManagement>

        <dependencies>

            <!-- Serializzazione JSON (Jackson) -->

            <dependency>

                <groupId>com.fasterxml.jackson.core</groupId>

                <artifactId>jackson-databind</artifactId>

                <version>${jackson.version}</version>

            </dependency>

            <dependency>

                <groupId>com.fasterxml.jackson.core</groupId>

                <artifactId>jackson-core</artifactId>

                <version>${jackson.version}</version>

            </dependency>

            <dependency>

                <groupId>com.fasterxml.jackson.core</groupId>

                <artifactId>jackson-annotations</artifactId>

                <version>${jackson.version}</version>

            </dependency>

            <dependency>

                <groupId>com.fasterxml.jackson.datatype</groupId>

                <artifactId>jackson-datatype-jsr310</artifactId>

                <version>${jackson.version}</version>

            </dependency>


            <!-- Framework di Testing -->

            <dependency>

                <groupId>org.junit.jupiter</groupId>

                <artifactId>junit-jupiter</artifactId>

                <version>5.10.0</version>

                <scope>test</scope>

            </dependency>

        </dependencies>

    </dependencyManagement>


    <!--

      Gestione centralizzata dei plugin per uniformare il processo di 

      compilazione ed esecuzione tra i moduli client e server.

    -->

    <build>

        <pluginManagement>

            <plugins>

                <plugin>

                    <groupId>org.apache.maven.plugins</groupId>

                    <artifactId>maven-compiler-plugin</artifactId>

                    <version>3.11.0</version>

                    <configuration>

                        <source>${java.version}</source>

                        <target>${java.version}</target>

                    </configuration>

                </plugin>

                <plugin>

                    <groupId>org.apache.maven.plugins</groupId>

                    <artifactId>maven-shade-plugin</artifactId>

                    <version>3.5.0</version>

                </plugin>

                <plugin>

                    <groupId>org.openjfx</groupId>

                    <artifactId>javafx-maven-plugin</artifactId>

                    <version>0.0.8</version>

                </plugin>

            </plugins>

        </pluginManagement>

    </build>

</project> 